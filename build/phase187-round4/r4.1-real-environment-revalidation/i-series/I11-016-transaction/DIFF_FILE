diff --git a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase140_7Validation.cs b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase140_7Validation.cs
index e84360a84..3aa1444a8 100644
--- a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase140_7Validation.cs
+++ b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase140_7Validation.cs
@@ -162,8 +162,10 @@ namespace Unity.FoxgloveSDK.Tests
 
             producer.Start();
             consumer.Start();
-            producer.Join();
-            consumer.Join();
+            if (!producer.Join(TimeSpan.FromSeconds(10)))
+                throw new TimeoutException("DropOldestBoundedQueue producer did not terminate within 10 seconds.");
+            if (!consumer.Join(TimeSpan.FromSeconds(10)))
+                throw new TimeoutException("DropOldestBoundedQueue consumer did not terminate within 10 seconds.");
 
             Check(errors.Count == 0 && consumed > 0,
                 "140-7E-1: DropOldestBoundedQueue tolerates concurrent enqueue/dequeue stress");
diff --git a/Scripts/release/regression_checks/test_release_tooling.py b/Scripts/release/regression_checks/test_release_tooling.py
index 7384919b3..daf7edaf8 100644
--- a/Scripts/release/regression_checks/test_release_tooling.py
+++ b/Scripts/release/regression_checks/test_release_tooling.py
@@ -1228,6 +1228,12 @@ class RunCiTests(unittest.TestCase):
         self.assertIn("catch (Exception)", source)
         self.assertIn("return GetDefaultDir();", source)
 
+    def test_phase140_queue_stress_bounds_thread_joins(self) -> None:
+        """Concurrent queue validation must not hang indefinitely on a stalled worker."""
+        source = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Tests" / "Runtime" / "Phase140_7Validation.cs").read_text(encoding="utf-8")
+        self.assertIn("producer.Join(TimeSpan.FromSeconds", source)
+        self.assertIn("consumer.Join(TimeSpan.FromSeconds", source)
+
     def test_phase184_acceptance_regressions_have_a_truthful_dedicated_lane(self) -> None:
         """The selector must execute exactly six pure unittest suites in locked order."""
         with mock.patch.object(self.run_ci, "run", return_value=True) as run:
