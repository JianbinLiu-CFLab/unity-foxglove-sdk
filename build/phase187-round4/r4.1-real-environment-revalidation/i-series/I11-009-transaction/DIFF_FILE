diff --git a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase134_1Validation.cs b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase134_1Validation.cs
index 86df3b9e9..fd270ee0e 100644
--- a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase134_1Validation.cs
+++ b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase134_1Validation.cs
@@ -289,7 +289,10 @@ namespace Unity.FoxgloveSDK.Tests
                 throw new InvalidOperationException("Could not find repository root.");
 
             var path = Path.Combine(root, relativePath.Replace('/', Path.DirectorySeparatorChar));
-            return File.Exists(path) ? File.ReadAllText(path) : string.Empty;
+            if (!File.Exists(path))
+                throw new FileNotFoundException("Required validation source is missing.", path);
+
+            return File.ReadAllText(path);
         }
 
         private sealed class MutablePayload
diff --git a/Scripts/release/regression_checks/test_release_tooling.py b/Scripts/release/regression_checks/test_release_tooling.py
index 1196ed3b7..f772b2d36 100644
--- a/Scripts/release/regression_checks/test_release_tooling.py
+++ b/Scripts/release/regression_checks/test_release_tooling.py
@@ -1199,6 +1199,12 @@ class RunCiTests(unittest.TestCase):
         self.assertIn("if (!IsValidFoxRunHash(hash))", source)
         self.assertIn("return string.Empty;", source)
 
+    def test_phase134_read_repo_text_rejects_missing_source(self) -> None:
+        """Provider validation must fail closed when a required source file is absent."""
+        source = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Tests" / "Runtime" / "Phase134_1Validation.cs").read_text(encoding="utf-8")
+        self.assertIn("if (!File.Exists(path))", source)
+        self.assertIn("throw new FileNotFoundException", source)
+
     def test_phase184_acceptance_regressions_have_a_truthful_dedicated_lane(self) -> None:
         """The selector must execute exactly six pure unittest suites in locked order."""
         with mock.patch.object(self.run_ci, "run", return_value=True) as run:
