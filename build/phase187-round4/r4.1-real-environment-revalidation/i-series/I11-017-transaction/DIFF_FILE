diff --git a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase13Validation.cs b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase13Validation.cs
index fb3627a4c..4a6224a03 100644
--- a/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase13Validation.cs
+++ b/Packages/dev.unity2foxglove.sdk/Tests/Runtime/Phase13Validation.cs
@@ -103,12 +103,23 @@ namespace Unity.FoxgloveSDK.Tests
         static void TestRecordingController()
         {
             var rt = new FoxgloveRuntime();
-            Assert(rt.RecordingEnabled == false, "RecordingEnabled defaults to false");
-            rt.EnableRecording(Path.GetTempFileName(), 1024 * 1024, "", "LeftHand");
-            Assert(rt.RecordingEnabled == true, "RecordingEnabled after Enable");
-            rt.DisableRecording();
-            Assert(rt.RecordingEnabled == false, "RecordingEnabled after Disable");
-            rt.Dispose();
+            var recordingPath = Path.GetTempFileName();
+            try
+            {
+                Assert(rt.RecordingEnabled == false, "RecordingEnabled defaults to false");
+                rt.EnableRecording(recordingPath, 1024 * 1024, "", "LeftHand");
+                Assert(rt.RecordingEnabled == true, "RecordingEnabled after Enable");
+                rt.DisableRecording();
+                Assert(rt.RecordingEnabled == false, "RecordingEnabled after Disable");
+            }
+            finally
+            {
+                rt.Dispose();
+                if (File.Exists(recordingPath))
+                    File.Delete(recordingPath);
+            }
+
+            Assert(!File.Exists(recordingPath), "Recording fixture path is cleaned after controller disposal");
         }
 
         // Helper: create minimal MCAP in memory stream, write to temp file, return path
diff --git a/Scripts/release/regression_checks/test_release_tooling.py b/Scripts/release/regression_checks/test_release_tooling.py
index 90da4280c..6757bad27 100644
--- a/Scripts/release/regression_checks/test_release_tooling.py
+++ b/Scripts/release/regression_checks/test_release_tooling.py
@@ -1211,6 +1211,12 @@ class RunCiTests(unittest.TestCase):
         self.assertIn("if (!File.Exists(path))", source)
         self.assertIn("throw new FileNotFoundException", source)
 
+    def test_phase13_recording_fixture_owns_and_cleans_temp_path(self) -> None:
+        """Recording validation must retain the temp-path identity through cleanup."""
+        source = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Tests" / "Runtime" / "Phase13Validation.cs").read_text(encoding="utf-8")
+        self.assertIn("var recordingPath = Path.GetTempFileName()", source)
+        self.assertIn("File.Delete(recordingPath)", source)
+
     def test_phase184_acceptance_regressions_have_a_truthful_dedicated_lane(self) -> None:
         """The selector must execute exactly six pure unittest suites in locked order."""
         with mock.patch.object(self.run_ci, "run", return_value=True) as run:
