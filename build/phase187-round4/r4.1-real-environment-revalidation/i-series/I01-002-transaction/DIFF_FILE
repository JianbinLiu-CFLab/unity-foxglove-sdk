diff --git a/Packages/dev.unity2foxglove.sdk/Editor/Shared/FoxRunEncodingEditorLabels.cs b/Packages/dev.unity2foxglove.sdk/Editor/Shared/FoxRunEncodingEditorLabels.cs
index 157ae89db..27f276d50 100644
--- a/Packages/dev.unity2foxglove.sdk/Editor/Shared/FoxRunEncodingEditorLabels.cs
+++ b/Packages/dev.unity2foxglove.sdk/Editor/Shared/FoxRunEncodingEditorLabels.cs
@@ -6,6 +6,7 @@
 
 using Unity.FoxgloveSDK.Components;
 using UnityEditor;
+using UnityEngine;
 
 namespace Unity.FoxgloveSDK.Editor
 {
@@ -18,21 +19,31 @@ namespace Unity.FoxgloveSDK.Editor
             if (property == null)
                 return;
 
-            var selected = property.intValue switch
+            var rawValue = property.intValue;
+            var supported = rawValue == (int)FoxRunEncoding.Protobuf
+                || rawValue == (int)FoxRunEncoding.JSON
+                || rawValue == (int)FoxRunEncoding.MessagePack;
+            if (!supported)
+                EditorGUILayout.HelpBox($"Stored FoxRun encoding value {rawValue} is unsupported. Choose an explicit replacement.", MessageType.Warning);
+
+            var selected = rawValue switch
             {
                 (int)FoxRunEncoding.Protobuf => 0,
                 (int)FoxRunEncoding.JSON => 1,
                 (int)FoxRunEncoding.MessagePack => 2,
-                _ => 0
+                _ => -1
             };
+            EditorGUI.BeginChangeCheck();
             selected = EditorGUILayout.Popup(label, selected, ManagerDefaultLabels);
-            property.intValue = selected switch
+            if (EditorGUI.EndChangeCheck() && selected >= 0 && selected < ManagerDefaultLabels.Length)
             {
-                0 => (int)FoxRunEncoding.Protobuf,
-                1 => (int)FoxRunEncoding.JSON,
-                2 => (int)FoxRunEncoding.MessagePack,
-                _ => (int)FoxRunEncoding.Protobuf
-            };
+                property.intValue = selected switch
+                {
+                    0 => (int)FoxRunEncoding.Protobuf,
+                    1 => (int)FoxRunEncoding.JSON,
+                    _ => (int)FoxRunEncoding.MessagePack
+                };
+            }
         }
 
         public static string ToDisplayLabel(FoxRunEncoding encoding)
diff --git a/Scripts/release/regression_checks/test_release_tooling.py b/Scripts/release/regression_checks/test_release_tooling.py
index 1da512246..47a9bfc5e 100644
--- a/Scripts/release/regression_checks/test_release_tooling.py
+++ b/Scripts/release/regression_checks/test_release_tooling.py
@@ -1199,6 +1199,14 @@ class RunCiTests(unittest.TestCase):
         self.assertIn("if (!IsValidFoxRunHash(hash))", source)
         self.assertIn("return string.Empty;", source)
 
+    def test_encoding_inspectors_preserve_unsupported_serialized_values(self) -> None:
+        """Enum inspectors must not rewrite forward-compatible raw values on repaint."""
+        foxrun = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Editor" / "Shared" / "FoxRunEncodingEditorLabels.cs").read_text(encoding="utf-8")
+        publisher = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Editor" / "Shared" / "PublisherEncodingEditorLabels.cs").read_text(encoding="utf-8")
+        self.assertIn("EditorGUI.BeginChangeCheck", foxrun)
+        self.assertIn("EditorGUI.EndChangeCheck", foxrun)
+        self.assertIn("EditorGUI.BeginChangeCheck", publisher)
+
     def test_phase134_read_repo_text_rejects_missing_source(self) -> None:
         """Provider validation must fail closed when a required source file is absent."""
         source = (ROOT / "Packages" / "dev.unity2foxglove.sdk" / "Tests" / "Runtime" / "Phase134_1Validation.cs").read_text(encoding="utf-8")
